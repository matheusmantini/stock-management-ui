import React from 'react';
import { ContainerLoading, LoadingSlider } from './styled';

const Loading = () => {
  return (
    <ContainerLoading>
      <LoadingSlider />
    </ContainerLoading>
  );
}

export default Loading;